/** Shared props of the six coin glyphs (SPEC §11.10). */
export interface CoinGlyphProps {
  /** Render size in px — 28 in the selector, 24 in the dropdown, 22 in summary rows. */
  size: number;
  className?: string;
}
